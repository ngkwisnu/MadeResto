import { validateLoginAccount } from "./login.validation.js";
import { validateRegisterAccount } from "./register.validation,js";

export default auth_validation = {
  validateLoginAccount,
  validateRegisterAccount,
};
